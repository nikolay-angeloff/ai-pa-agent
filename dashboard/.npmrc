# Force the public registry for this project, overriding any corporate
# registry configured in a user/global .npmrc (e.g. a company Artifactory) —
# this repo only ever pulls public packages, and the Docker build has no
# access to corporate registry credentials.
registry=https://registry.npmjs.org/
