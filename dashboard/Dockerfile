# Phase 0 placeholder — nginx serving a static page.
# Replace with multi-stage Angular build in Phase 4.
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 4200
